import { createGlobalStyle } from "styled-components";

export default createGlobalStyle`

  body{
      margin: 0;
      padding: 0; 
    }
  *:root {
    --brand-color: #152d73;
    --dark-color: #0f172a;
    --mid-color: #cbd5e1;
    --light-color: #f8fafc;
  }
`;
